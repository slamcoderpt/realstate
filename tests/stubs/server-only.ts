// Stub vazio: nos testes (Node/vitest) não há bundle de cliente, pelo que o
// guard do pacote `server-only` — que lança ao ser importado fora de um React
// Server Component — não se aplica. Aliased em vitest.config.ts.
export {};
